apiVersion: v1
kind: Secret
metadata:
  name: chemchat-secrets
  namespace: chemchat
  labels:
    app.kubernetes.io/name: chemchat
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme-production"
  JWT_SECRET: "changeme-jwt-secret-production-256bit"
  REDIS_PASSWORD: "changeme-redis-production"
  AWS_ACCESS_KEY_ID: "changeme-aws-access-key"
  AWS_SECRET_ACCESS_KEY: "changeme-aws-secret-key"
  FIREBASE_PRIVATE_KEY: "changeme-firebase-private-key"
  SMTP_PASSWORD: "changeme-smtp-password"
---
apiVersion: v1
kind: Secret
metadata:
  name: chemchat-secrets-staging
  namespace: chemchat-staging
  labels:
    app.kubernetes.io/name: chemchat
    app.kubernetes.io/component: secrets
    app.kubernetes.io/instance: staging
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme-staging"
  JWT_SECRET: "changeme-jwt-secret-staging-256bit"
  REDIS_PASSWORD: "changeme-redis-staging"
  AWS_ACCESS_KEY_ID: "changeme-aws-access-key-staging"
  AWS_SECRET_ACCESS_KEY: "changeme-aws-secret-key-staging"
  FIREBASE_PRIVATE_KEY: "changeme-firebase-private-key-staging"
  SMTP_PASSWORD: "changeme-smtp-password-staging"
